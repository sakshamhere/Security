# Remote Desktop Protocol

GUI remote access protocol developed by microsoft to remotly connect to windows systems

RDP uses `TCP port 3389` by default but can also be configured to be used on different TCP port

RDP requires legitimate credentials in clear text , it can be brute forced