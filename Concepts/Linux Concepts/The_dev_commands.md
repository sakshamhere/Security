

# /dev

In the Linux file system, everything is a file or a directory,  Even devices are accessed as files.

`/dev `is a directory that stores all the physical and virtual devices of a Linux system

# /dev/null

